bin.name = "diagrustic"
args = ["mismatched-types"]
stdout = ""
stderr = """
error[E0308]: mismatched types
 --> test.rs:1:14
  |
1 | let x: i32 = "hello";
  |        ---   ^^^^^^^ expected `i32`, found `&str`
  |        |
  |        due to this type annotation `i32`
  |
help: change the type to `&str`
  |
1 - let x: i32 = "hello";
1 + let x: &str = "hello";
  |

"""
